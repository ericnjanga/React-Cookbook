// src/wdyr.js
// import React from 'react';

/** WDYR is only active in development mode, which ensures that it won’t affect performance in production builds. */
// if (process.env.NODE_ENV === 'development') {
//   const whyDidYouRender = require('@welldone-software/why-did-you-render');
//   whyDidYouRender(React, {
//     trackAllPureComponents: true,  // Tracks all pure components
//     logOnDifferentValues: true,    // Log when props or state are different
//     // include: [/./],                // Regex to include all components
//     include: [/^App/, /^AppRoutes/, /^LayoutGlobal/, /^PageHome/, /^LayoutSubpage/, /^Nav/, /^Breadcrumbs/, /^Switch/],    
//     exclude: [/^NavLink/],           
//   });
// }
